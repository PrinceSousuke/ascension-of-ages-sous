# ============================================================================
# Stage: Otherworldly
# ============================================================================
# CHOKEPOINT MODEL (rewritten 2026-05-02). BetterEnd / Phantasm are NOT moved
# here despite pack memory noting "OW boundary redesign" — the SNBT quest
# content for those mods is still authored at Renaissance (r_end_the_void_beyond
# quests aeternium_sword + eternal_pedestal + crystal_shard), so moving the
# locks would create new quest-progression bugs. Their chokepoints stay at
# the_renaissance.toml until the quest redesign happens.
# ============================================================================

[stage]
id = "otherworldly"
display_name = "Otherworldly"
description = "Launch infrastructure, off-world survival, and post-launch logistics."
icon = "stellaris:rocket_nose_cone"
unlock_message = "&9&lOtherworldly Unlocked! &r&7Launch, oxygen, and off-world progression are now open."
dependency = "atomic"

[locks]
items = [
  "stellaris:rocket",
  # NOTE: betterend:aeternium_sword REMOVED — quested in r_end_the_void_beyond
  # (Renaissance chapter); demoted from OW to Renaissance natural recipe gating.
  # ─── Occultism Otherworldly — book_of_binding_marid (2026-05-03). Activation-
  # item chokepoint for Marid-tier rituals: craft_miner_marid_master (master miner,
  # the apex Occultism resource-gen tier), summon_marid_crusher / crystallizer /
  # smelter (endgame processing). Marid = endgame spirit tier. Pairs cleanly with
  # Otherworldly's Otherworld dimension content theme.
  "occultism:book_of_binding_marid"
]
item_tags = []
item_mods = []
blocks = []
block_tags = []
block_mods = []
dimensions = []
fluids = []
fluid_tags = []
fluid_mods = []
mods = []
names = []
entities = []
entity_tags = []
entity_mods = []
unlocked_items = []
unlocked_blocks = []
unlocked_entities = []
unlocked_fluids = []
