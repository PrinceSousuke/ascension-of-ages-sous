# 02e — ATOMIC: COMPLETION

**Owner:** Fable → Opus → CC. **Prepend:** `01_MASTER_PREAMBLE`. **Target list:** the "Atomic"
section of `02a`. **Depends on:** `LEDGER.md` signed.

Atomic's existing chapters (273 quests) are dense — nuclearscience, createnuclear, MI-nuclear,
the macabre boss spine, oritech convergence are DONE. Gaps are endgame-mod tiers that are
gated to Atomic but never taught.

### P2 — the unquested Atomic tiers
- **draconicevolution**: the entire **IO / relay / wireless crystal** energy-transfer
  subsystem = 0 at any tier; plus `particle_generator`, `grinder`, `disenchanter`,
  `celestial_manipulator`, `dislocation_inhibitor`, `draconium_chest`. This subsystem also
  spans OW/Ascension (`04`/`05`) — author the Atomic base tier here and let the higher tiers
  chain in the later files.
- **ballistix**: `nuclear` + `thermobaric` warheads themselves (0 quests — the cross-gate
  softlock is fixed but the payoff is unquested) + a representative subset of the remaining
  warhead roster. Detection/launch infra is already taught.
- **evolvedmekanism**: one more factory family beyond enriching + the dense personal-storage
  tier (currently 6 of 100+ gated ids taught — it reads as a sample, not a ladder).
- **mekmm**: 2–3 more large-factory representatives beyond `basic_oxidizing_factory`.

### P3 — small fills
- **cabletiers**: mega tier (7 ids).
- **extrastorage**: `netherite_crafter`/`advanced_importer` if not closed at Gilded.
- **appmek**: any remaining `chemical_storage_cell` tiers.
- **nuclearscience**: `radioisotopegenerator`; confirm full hazmat armor set beyond helmet.
- [CANON] **Mekanism SPS** (`sps_casing`/`sps_port`) — ungated + unquested; decide whether
  antimatter production is in scope (decision queue).

## Method
Same five steps. These are mostly extensions of existing Atomic chapters; keep first-placement-
wins, no dupes. For Draconic, author the base tier such that OW/Ascension tiers cleanly depend
on it. Crossing-free; stub briefs; weaves via `06`.

## Deliverables
Edited Atomic chapters, VERIFICATION LOG, stub briefs, before/after counts to `LEDGER.md`,
[CANON] items to the `08` queue.
