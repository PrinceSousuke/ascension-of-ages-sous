# 02b — RENAISSANCE + EARLIER-AGE COMPLETION

**Owner:** Fable (structure) → Opus (prose) → CC (age-discipline + merge). **Prepend:**
`01_MASTER_PREAMBLE`. **Target list:** the "Renaissance and earlier" section of `02a`.
**Depends on:** `LEDGER.md` (from 02) signed.

These ages read as "done" but hide the pack's single highest-priority magic gap. Do not add
density for its own sake here — these are targeted foundation + optional-line fills.

## P1 — Spectrum front door (the pack's worst magic gap) [SPINE]
Spectrum is a stated main magic pillar, Renaissance-gated, yet its entire entry mechanic is
unquested (`spectrum:pedestal*` = 0 across all chapters, hand-verified). Author, in
`ren_magic_foundations` (or a dedicated Spectrum sub-chapter if layout crosses):
- the pedestal-crafting intro: `pedestal_all_basic` → `pedestal_basic_*` tier → `pedestal_moonstone`
  → `pedestal_onyx`, plus the 5 network nodes (verify exact ids in the jar).
- the hue mechanic: `color_picker` + `titration_barrel` (how ink/pigment colours are made).
Wire the existing (already-good) fusion-shrine / spirit-instiller / Deeper Down content to
depend on this intro so the chapter reads foundation → apparatus → endgame.

## P2 — required side/spine fills
- **Immersive Engineering onboarding** (Medieval): `coke_oven` + `tank` intro (0 quests). This
  is the earliest IE gate and the entry to the whole IR multiblock line in `02c`.
- **Malum** [SPINE]: keep the spirit_altar/soulstone intro; add a short progression for the 5
  elemental Spirit types (wicked/sacred/infernal/earthen/aqueous) and `ritual_plinth`. Verify
  whether the 5 types need a gate (currently none). Depths of Malum dimension → decision queue.
- **Neo Vitae** (Renaissance): the altar/blood-orb intro is real; leave it, but confirm it
  chains cleanly into the IR Neo Vitae segment authored in `02c`.

## P3 — optional + canon-gated
- **apothic_enchanting** [CANON]: ungated + 0 quests, but canon wants "late-Renaissance
  controlled." This needs a gate first (decision queue, `08`), then a small controlled line.
  Do NOT author until the gate call is made.
- **mahoutsukai** [CANON]: optional-fun; build a small, explicitly-optional Renaissance chapter
  (metadata flagged optional, never a dependency of any capstone). Verify magic-circle /
  familiar item ids in the jar before authoring — do not invent them.

## Method (same for every age file)
1. For each target mod: re-verify its gate age + confirm each item id against the jar
   (`unzip -l`/recipe grep). Log it.
2. Extend the existing chapter where one exists (first-placement-wins, no duplicates); add a
   new chapter only when a whole mod has no home and layout would otherwise cross.
3. Foundations-first: onboarding rungs before capstone; then re-point the existing endgame
   quests to depend on the new onboarding so the graph teaches in order.
4. Crossing-free layout (hard). Stub lang keys + teaching briefs for Opus.
5. Any cross-mod recipe need → `06` weaves. Re-run the tier audit on touched chapters.

## Deliverables
Edited/new Renaissance-area chapters, VERIFICATION LOG, stub briefs, before/after counts to
`LEDGER.md`, and every [CANON] item pushed to the `08` decision queue (not authored).
