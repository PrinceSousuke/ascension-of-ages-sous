# 02d — GILDED AGE: COMPLETION

**Owner:** Fable → Opus → CC. **Prepend:** `01_MASTER_PREAMBLE`. **Target list:** the "Gilded
Age" section of `02a`. **Depends on:** `LEDGER.md` signed; ideally after `02c` so IR
foundations exist to build on.

Gilded's existing chapters (368 quests) are dense on MI/IE-Gilded/electrodynamics/PneumaticCraft
and those are genuinely DONE. The gaps are targeted mid-tier fills plus one spine hole.

### P1 — Neo Vitae orb ladder [SPINE][FOUND]
The blood_orb progression jumps from apprentice (Renaissance) to transcendent (Atomic) with
the **magician / master / archmage** tier entirely missing (0 quests despite Gilded gate).
Author that ladder as a Gilded Neo Vitae beat so the magic spine is continuous. Also the
Sentient-gear line if it isn't fully covered — verify current coverage first.

### P2 — mid-tier machine fills
- **industrialforegoing**: the unquested Gilded machines — `sludge_refiner`, `sewer`/
  `sewage_composter`, `washing_factory`, `water_condensator`, `potion_brewer`, `stasis_chamber`,
  `spores_recreator`, `conveyor`, `infinity_charger`. Extend the existing IF chapter.
- **extrastorage**: `netherite_crafter`, `advanced_importer`.
- **appmek** (Applied Mekanistics): remaining `chemical_storage_cell` tiers (1k/4k/16k/256k) +
  confirm the `chemical_cell_housing` intro is taught.
- **flight/propulsion (Create Phase 3)**: cross-reference `08_CREATE_GAPS_AND_QA` — the Gilded
  flight line (Aeronautics + Propulsion) lands here. Don't double-author; that file owns it,
  this file just reserves the g-slot and the dependency link.

### P3 — polish + canon
- **enderio**: `fluid_tank`/`pressurized_fluid_tank`; [CANON] `powered_spawner` (ungated +
  unquested — decision queue).
- **mekanismadditions** [CANON]: 0 gate + 0 quests — check the jar's actual contents, then
  decide chapter slot vs explicit skip (decision queue).

## Method
Same five steps as `02b`/`02c`. Extend existing Gilded chapters; foundations-first for the Neo
Vitae ladder; crossing-free; stub briefs; weaves via `06`; re-run tier audit on touched files.

## Deliverables
Edited Gilded chapters, VERIFICATION LOG, stub briefs, before/after counts to `LEDGER.md`,
[CANON] items to the `08` queue.
